import '../styles/globals.css';
import type { AppProps } from 'next/app';

const Architeuthis: React.FC<AppProps> = ({ Component, pageProps }) => <Component {...pageProps} />;

export default Architeuthis;
