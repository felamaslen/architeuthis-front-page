import styled from '@emotion/styled';

export const NavDropdown = styled.ul`
    display: flex;
    margin-bottom: 1em;
    flex-flow: column;

    li {
        padding: 0.3em 0;
        transition: background 0.3s ease;
    }
`;
